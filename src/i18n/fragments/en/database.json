{
  "database.overview.pageTitle": "Database overview",
  "database.overview.refresh": "Refresh",
  "database.overview.loading": "Loading the database details …",
  "database.overview.noArea": "The database overview is only available inside an area.",
  "database.overview.noDatabase": "This area holds no database. One comes into being with the first document that carries a fact sheet.",
  "database.overview.indexing": "The area index is being built …",
  "database.overview.section.profile": "Fact sheet",
  "database.overview.section.tables": "Tables",
  "database.overview.section.issues": "Issues",
  "database.overview.name": "Name",
  "database.overview.description": "Description",
  "database.overview.noName": "no name",
  "database.overview.noDescription": "no description",
  "database.overview.col.table": "Table",
  "database.overview.col.fields": "Fields",
  "database.overview.noTables": "This database holds no table yet.",
  "database.overview.noIssues": "No issues.",
  "database.hint.location": "Field {index} ({name})",
  "database.hint.unknown": "Issue without a description ({name}).",
  "database.hint.container": "db-table is not an object; this table provides no fields.",
  "database.hint.fieldsNotList": "fields is not a list; this table provides no fields.",
  "database.hint.entry": "{ort}: not a valid entry; an object with name is expected.",
  "database.hint.name": "{ort}: field name missing; name is the only required property.",
  "database.hint.duplicate": "{ort}: duplicate field name; it must be unique within the table.",
  "database.hint.type": "{ort}: unknown column type; expected one of {expected}.",
  "database.hint.label": "{ort}: label cannot be read; expected text or a mapping of language to text.",
  "database.hint.required": "{ort}: required is not a boolean.",
  "database.hint.typeComputed": "{ort}: a computed field is excluded as a column; possible is one of {expected}.",
  "database.hint.typeStructured": "{ort}: a structured value is excluded as a column; possible is one of {expected}.",
  "database.hint.multipleColumn": "{ort}: a multi-valued column is excluded; possible is one of {expected}.",
  "database.hint.values": "{ort}: invalid value list; expected a list of simple values matching the type.",
  "database.hint.default": "{ort}: default value does not match the type; expected a value of type {expected}.",
  "database.hint.defaultOutsideValues": "{ort}: default value outside the value range; allowed is one of {expected}.",
  "database.hint.options": "{ort}: options is not an object.",
  "database.hint.optionUnknown": "{ort}: unknown entry in options; {expected} is provided. The entry is dropped, the column remains.",
  "database.hint.optionValue": "{ort}: entry in options has an unsuitable value; expected is {expected}. The entry is dropped, the column remains.",
  "database.hint.lastId": "lastId is not a valid mark; expected a whole number from zero upwards.",
  "database.hint.key": "key is not a key; expected a field name or a list of field names.",
  "database.hint.keyUnknown": "key names a field the definition does not know; present are {expected}.",
  "database.hint.display": "display is not a field name.",
  "database.hint.displayUnknown": "display names a field the definition does not know; present are {expected}.",
  "database.hint.databaseContainer": "db-database is not an object; the fact sheet of the database is dropped.",
  "database.hint.databaseName": "The name of the database is not a valid label; expected text or a mapping of language to text.",
  "database.hint.databaseDescription": "The description of the database is not a valid label; expected text or a mapping of language to text.",
  "database.hint.schemaVersion": "schemaVersion is not text.",
  "database.hint.fallbackLocale": "fallbackLocale is not a language code.",
  "database.hint.yaml": "YAML error in the frontmatter — the entries of this file are dropped.",
  "database.hint.duplicateTable": "Several files claim the table name {name}; all of them remain visible and carry this notice.",
  "database.hint.duplicateDatabase": "A second fact sheet lies in {name}; the database is described by the first one.",
  "database.hint.recordStrayContent": "Record {position}: text outside any cell; it stays in the file and does not appear in the table.",
  "database.hint.recordStrayContent.ohneOrt": "Text outside any record; it stays in the file and does not appear in the table.",
  "database.hint.recordCellsMissing": "Record {position}: fewer cells than fields; {expected} are expected, the missing ones stay empty.",
  "database.hint.recordCellsExtra": "Record {position}: more cells than fields; {expected} are expected, the surplus ones stay in the file and do not appear in the table.",
  "database.hint.recordNoDefinition": "The file does not describe a table; without a definition in the front matter the records yield no columns.",
  "database.hint.recordIdInvalid": "Record {position}: the identifier on the record marker cannot be read; an id entry with an identifier such as r-00001 is expected. The text stays unchanged.",
  "settings.database.title": "Database",
  "settings.database.hint": "This area holds a database. Name and description come from the fact sheet, the tables from their definitions; both are maintained in the area itself and are only shown here.",
  "settings.database.loading": "Loading database details …",
  "settings.database.name": "Name",
  "settings.database.description": "Description",
  "settings.database.tables": "Tables",
  "settings.database.issues": "Issues",
  "settings.database.noName": "no name",
  "settings.database.noDescription": "no description",
  "settings.database.noIssues": "none",
  "settings.database.overviewOnOpen": "Show the overview when the area is opened",
  "settings.database.writeFailed": "The setting could not be saved. The area file is damaged and is never overwritten."
}
